world {
    size = 10[edge=foo], infinite
    tickrate = 120
}

state foo {
    let a = (not (2 - 3) / (4)) is 2;

    if (23) {
        let b = 3;
        if (not b) {
            become foo;
        }
    }

    become bar;
}
