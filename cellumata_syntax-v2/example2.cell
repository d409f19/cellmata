board {
    world = 10[edge=foo], infinite
    tickrate = 120
}

const baz = [2]bool{ true, false }

state foo {
    let a = (not (2 - 3) / (4)) is 2;

    baz[0] = baz[1] - 2;

    if (23) {
        let b = 3;
        if (not b) {
            become foo;
        } else { become bar;}
    } else if (false) {
        become baz;
    } else {
        become foobar;
    }
}

