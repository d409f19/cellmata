world {
    size = 100 [wrap], 200 [wrap];
    tickrate = 1;
    cellsize = 5;
}

state ident (0, 0, 0) {
    // Should work, as ints would be implicitly converted to floats before call
    let y = pow(1.5, 5);
    become ident;
}
