world {
    size = 100 [wrap], 200 [wrap];
    tickrate = 1;
    cellsize = 5;
}

state ident (0, 0, 0) {
    // Assumes that the semantics of a hashtag is also the id of a given state in a non-multi-state state, which would be 0
    if (# == 2) {
        become ident;
    }

    become ident;
}
