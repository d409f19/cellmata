//contains unreachable code that should produce warning, but no error
//Should be moved to non-compiling programs as soon as errorlogger contains warnings
//batchtest should also include warning assertions

world {
    size = 100 [wrap], 200 [wrap]
    tickrate = 1
    cellsize = 5
}

state ident (0, 0, 0) {
    become ident;
}

function f1(int a) int {
    for (let x = a; x < a; x = x+1) {
        for (let y = a; y < a; x = y+1) {
            break;
            let b = a; // unreachable
        }
        return a;
    }
}