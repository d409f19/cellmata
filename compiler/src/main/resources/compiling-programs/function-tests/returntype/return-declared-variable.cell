//Currently functions must have parameters, when issue #108 is fixed, the parameter can be removed

world {
    size = 100 [wrap], 200 [wrap]
    tickrate = 1
    cellsize = 5
}

state ident (0, 0, 0) {
    become ident;
}

function f(int a) bool {
    let c = true;
    return c;
}