world {
    size = 100 [wrap], 200 [wrap];
    tickrate = 1;
    cellsize = 5;
}

state ident (0, 0, 0) {
    become ident;
}

function f(int a) int {
    for (let x = a; x < a; x = x+1) {
        for (let y = a; y < a; x = y+1) {
            continue;
        }
    }
    return a;
}