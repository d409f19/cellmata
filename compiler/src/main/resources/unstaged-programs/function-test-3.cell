world {
    size = 100 [wrap], 200 [wrap]
    tickrate = 1
    cellsize = 5
}

state ident (0, 0, 0) {
    become ident;
}

function f3(int a, int b) bool {
    let c = true;
    for (let x = a; x < b; x = x+1) {
        if ( x == 5 ) {
            return c && false;
        }
    }
    return c && !c;
}